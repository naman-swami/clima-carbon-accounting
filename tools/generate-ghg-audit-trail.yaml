annotations:
  cost: none
  read_only: true
  requires_confirmation: false
description: Export cryptographic hash verification for baseline inventory restatements
  and offset certificates.
input_schema:
  additionalProperties: false
  properties:
    reporting_year:
      type: number
    scope_selection:
      type: string
  required:
  - reporting_year
  type: object
name: generate-ghg-audit-trail
output_schema:
  additionalProperties: false
  properties:
    confidence:
      type: number
    data:
      type: string
    status:
      type: string
  required:
  - status
  - data
  type: object
